# Pyrnova decision record

Durable decisions are append-only. A later decision must identify what it supersedes.

| ID | Date | Decision | Rationale / consequence |
|---|---|---|---|
| D-001 | 2026-09 | Capture Radar is the active commercial wedge. | Keeps engineering tied to the first $100k; broader products remain deferred. |
| D-002 | 2026-09 | Deterministic state and evidence are authoritative; AI is bounded and human-gated. | Prevents model output from becoming fabricated fact. |
| D-003 | 2026-09 | Preserve point-in-time raw evidence and source-native identity. | Enables replay, audit, amendment history, and deterministic deduplication. |
| D-004 | 2026-09 | Use five evidence levels; Federal Register enrichment is capped at levels 1–2 and cannot create a candidate. | Prevents broad topical context from independently producing STRIKEs. |
| D-005 | 2026-09 | Freeze the accepted baseline as `scoring_v1`; every revision gets a new immutable version ID. | Preserves prior outputs and makes comparisons reproducible. |
| D-006 | 2026-09-08 | Reject `scoring_v2_candidate` after full-corpus comparison. | Its apparent precision gain came from demoting one case among only three STRIKEs; evidence was too sparse for adoption. |
| D-007 | 2026-09-08 | Close M3 with 20 cases across six mechanism families. | All 15 acceptance criteria passed; weaknesses remain explicit in the baseline report. |
| D-008 | 2026-09-08 | Live external calls are scarce infrastructure: archive once, replay many. | Protects quotas and makes offline development, replay, and acceptance roles explicit. |
| D-009 | 2026-09-08 | Keep the current flat Python/test layout until scale creates navigation cost. | Avoids churn with no behavioral benefit; conceptual boundaries are already clear. |
| D-010 | 2026-09-08 | Root authority documents supersede dated execution handovers and research. | Removes competing “canonical” claims and gives agents a short mandatory reading path. |
| D-011 | 2026-09-08 | M4 sources enter as conservative evidence/WATCH inputs; none independently manufactures a STRIKE. | More records must not weaken selectivity or bypass the existing evidence and human-review gates. |
| D-012 | 2026-09-08 | Program chains use explicit `program_key` or source-reference crosswalks across the eight M4 stages. | Partial and uncertain chains are representable without inferring relationships from topical similarity. |
| D-013 | 2026-09-08 | Adopt small adapter-neutral source controls, not a generic ingestion platform. | OFFLINE/LIVE-SAFE/ACCEPTANCE, request identity, budgets, accounting, retry metadata, and circuit state are reusable while transport stays source-specific. |
| D-014 | 2026-09-08 | Preserve `corpus_v1.json` and extend it with `corpus_m4.json`. | M3 remains reproducible while M4 can measure incremental source contribution and leave-one-source-out lift. |
| D-015 | 2026-09-08 | M5 cross-source joins are deterministic-first (`program_key`, native-identifier crosswalk); inference requires an explicit shared program identifier plus agency match. | Keeps chains evidence-gated; agency-name-only, topic-only, and chronology-only matches are rejected and counted, never materialized. |
| D-016 | 2026-09-08 | Relationships carry temporal/confidence provenance (`first_observed_at`, `join_method`, `confidence`, `rationale`); chain confidence is the weakest link, not a sum. | Lets replay answer "when could we first have known this relationship?" and keeps chain confidence simple, conservative, and explainable. |
| D-017 | 2026-09-08 | Opportunity transitions are derived by replaying `scoring_v1` point-in-time, not by a new scoring model. | Evolution history stays consistent with production scoring and never fabricates an unsupported promotion state. |
| D-018 | 2026-09-08 | Extend `corpus_m4.json` with a separate `corpus_m5.json` chain corpus; leave the M4 baseline frozen. | M4 scoring metrics stay reproducible while M5 validates multi-stage chains, rejected joins, and evolution. |
| D-019 | 2026-09-08 | Add an `appropriations` precursor source over explicitly configured official budget artifacts, distinguishing INTENT / AUTHORIZATION / FUNDING and emitting the most authoritative structured identifier (TAS → Federal Account → CFDA → program element → budget line item). | Moves Pyrnova earlier in the capital chain with deterministic identity and point-in-time availability, and supplies the structured anchors the inference model needs, without ingesting the whole budget universe. |
| D-020 | 2026-09-08 | Replace the M5 rubber-stamp inferred path with a weighted, explainable model gated by an authoritative structured **anchor**; keep the acceptance threshold frozen at 0.60. | Agency + topic + chronology + name-similarity are capped below 0.60 and can never combine into a join; only structured identity crosses the line. The canonical shared-identifier+agency case still scores exactly 0.60, so all M5 behavior is preserved. This gives the frozen threshold teeth rather than relaxing it. |
| D-021 | 2026-09-08 | Anchored inferred pairs scoring in `[0.45, 0.60)` are deferred to a human review queue, not linked; contradictions (agency conflict, temporal impossibility, geography/funding divergence) subtract and can invalidate an anchored pair. | Unknown stays unknown and ambiguous stays ambiguous; the persisted reviewer dispositions become calibration/training evidence rather than silent auto-joins. |
| D-022 | 2026-09-08 | Establish entity-level predicates (`AWARDED_TO`, `SUBSIDIARY_OF`, `LOCATED_AT`) only from authoritative structured fields (recipient/parent UEI, place of performance), never from topic. | Deepens the graph where primary evidence supports it without expanding vocabulary for theoretical completeness. |
| D-023 | 2026-09-08 | Extend `corpus_m5.json` with a separate `corpus_m6.json`; keep M2–M5 corpora frozen. Evaluate the 0.60 threshold by sweeping anchored candidates and leave it unchanged given the small sample. | M6 measures inferred precision, false-join rate, deferral, and precursor lead-time gain while earlier baselines stay reproducible; a threshold move requires materially more reviewed examples. |
| D-024 | 2026-09-08 | Represent a `CapitalCatalyst` as one object per resolved program chain (deterministic id; connected program keys collapse), referencing canonical signals/relationships rather than duplicating them. | Prevents duplicate catalysts from one underlying chain and keeps catalysts evidence-linked and replay-safe. |
| D-025 | 2026-09-08 | Keep `CommercialConsequence` a distinct object from `Opportunity`/STRIKE; a catalyst yields 0..N consequences and zero is a valid, common result. | Consequences carry the commercial reasoning (mechanism/directness/roles/capability/value/falsifiers) so STRIKE explainability improves without overloading the opportunity object; low consequence-to-catalyst ratio is healthy. |
| D-026 | 2026-09-08 | Adopt a deliberately small 7-family commercial mechanism taxonomy classified deterministically from stage + record_kind + source + explicit structured flags, never from topical similarity; a subsumption rule stops double-counting the same money. | Grounds commercial reasoning in evidence and forbids generic AI idea generation; DIRECT_PROCUREMENT subsumes the appropriation that funds it. |
| D-027 | 2026-09-08 | Directness doctrine: DIRECT → STRIKE-eligible (requires resolved buyer + specific capability); DOWNSTREAM → WATCH; SECOND_ORDER → internal/WATCH unless corroborated. | Prevents inferential second-order effects from becoming actionable STRIKEs and keeps selectivity. |
| D-028 | 2026-09-08 | Distinguish participant roles (FUNDING_AUTHORITY, PROGRAM_OWNER, BUYER, PRIME_RECIPIENT, BENEFICIARY, REGULATED_ENTITY) from authoritative structured fields; never infer SUPPLIER/SUBCONTRACTOR without explicit evidence. | "Buyer" is not one thing; who controls, owns, buys, receives, and benefits are different economic positions. |
| D-029 | 2026-09-08 | Capability classes must be specific (reject technology/consulting/services/manufacturing); value is KNOWN/ESTIMATED/BOUNDED/UNKNOWN with method+inputs+provenance+range and never an unsupported precise amount. | Supports future fit-matching and honest value reporting; unknown stays unknown. |
| D-030 | 2026-09-08 | Negative commercial evidence is structured (`ConsequenceFalsifier` with code + fatal flag), not a silent confidence reduction; catalyst/consequence/mechanism/capability confidences stay distinct from `scoring_v1`. | Falsification is auditable and truth/confidence stays separate from commercial attractiveness; `scoring_v1` remains frozen. Consequence precision graded only where consequence-level ground truth is declared. |
| D-031 | 2026-09-08 | Model an evidence-backed `CompanyProfile` (`pyrnova/company.py`) separate from the customer relevance profile in `match.py`; capabilities are normalized to specific labels via `capabilities.py` and built point-in-time. | Fit must rest on source-linked, specific capabilities that cannot leak future evidence; `match.py` (a `scoring_v1` input) is left untouched. |
| D-032 | 2026-09-08 | Fit is explicit capability-class overlap using the SAME normalizer for the consequence requirement and the company; agency-name-only, NAICS-only, keyword-only, and semantic-similarity-only fit are forbidden. | Prevents generic "same sector, seems like a fit" reasoning; a match is a shared specific capability class. |
| D-033 | 2026-09-08 | Fit dimensions report POSITIVE / NEGATIVE / UNKNOWN (never a forced neutral 0.5); capture posture is PRIME/SUPPORT/TEAM/DEFEND/NO_FIT with PRIME requiring full capability + eligibility + credible scale + prior prime performance; unknown/insufficient evidence is NO_FIT (`is_unknown`), never PRIME. | Keeps fit explainable and conservative; a NAICS/agency match as a subcontractor is a support signal, not a prime signal. |
| D-034 | 2026-09-08 | Fit confidence is kept strictly separate from `scoring_v1`, opportunity attractiveness, evidence confidence, and consequence confidence; blockers are structured (fatal forbids any fit, soft forbids PRIME). Fit precision is graded only where per-company fit ground truth is declared. | A strong opportunity can be a poor fit and vice versa; conflating them would corrupt selectivity. |
| D-035 | 2026-09-08 | Recover and stabilize the pre-existing internal Operations Panel as thin, local-only, loopback-only analyst tooling; make its tests sandbox-safe (skip live-socket bind; add a socket-free handler test). | Preserves useful internal tooling without a customer UI, portal, or new infrastructure, and keeps CI green under sandboxed sockets. |
| D-036 | 2026-09-08 | Ground real company profiles in archived USAspending prime-award evidence (keyless, public domain), archived once under `examples/real_evidence/` and replayed offline; start with Torch Technologies and Modern Technology Solutions. | Real, verifiable, official-government evidence replaces synthetic fixtures without a broad crawler or paid data; USAspending outranks secondary sources in the hierarchy. |
| D-037 | 2026-09-08 | `profile_as_of(company, cutoff)` filters EVERY profile fact (capabilities, scale, contract vehicles, buyer agencies, contract history) by `available_at <= cutoff`; award start date is used as `available_at`. No future award, vehicle, or capability may build an earlier profile. | Hard no-future-knowledge gate so a fit judgement uses only what was knowable at the time; a mega-award or new vehicle cannot inflate a prior profile. |
| D-038 | 2026-09-08 | Report real-profile fit metrics separately from synthetic-fixture metrics (`summarize_fit_results(source=...)`); never blend them into one number. | Prevents a small real sample from being flattered by synthetic cases, per the M9 calibration doctrine. |
| D-039 | 2026-09-08 | Certifications, security clearances, contract-vehicle breadth, and teaming access remain UNKNOWN unless authoritatively evidenced; TEAM posture is not exercised for real profiles because public award data does not reveal teaming agreements. | Unknown stays unknown; fit doctrine (no broad-sector/agency/NAICS/keyword/semantic-only match) is not loosened to manufacture real postures. |
| D-040 | 2026-09-09 | Resolve the orphan-clone "Operator Console v0.1" (commit `e9253b4`, never pushed to canonical; the artifact that had appeared missing) as **SUPERSEDED** by the canonical Operations Panel independently rebuilt/evolved through M8–M14. Port no orphan code: its second HTTP server + `console_static`, immutable per-run `RunStore` model, `quality`/`APPROVE·REJECT·HOLD` taxonomy, and ungoverned `acquisition.py` live-fetch all conflict with the current single state-centric panel and the M12–M14 governed ingestion (budgets/dedupe/circuit/archive-first). The canonical panel already provides — and exceeds (outcome lifecycle, M10–M14 company/source/chain intelligence) — its core workflow. Preserve the genuinely-useful *workflow concepts* (UI-triggered governed run, selective multi-candidate Signal Brief, richer queue filters) in `05-BACKLOG.md` + roadmap area 13, not as resurrected code. Changes nothing in scoring/fit. The original implementation is permanently archived on GitHub at `origin/archive/original-operator-console-v0.1` (commit `e9253b4`) as historical reference only — the canonical Operations Panel remains implementation authority; future work may consult the archive for UX/workflow ideas but must not treat it as active architecture, merge, or cherry-pick it. | One canonical panel, not two; useful UX preserved as concepts (governance rule: nothing strategically material silently dropped) without resurrecting stale architecture or violating the frozen ingestion/scoring doctrine. See D-035 (panel recovery) and `06-HISTORY.md`. |

| D-041 | 2026-09-09 | **Product Language Authority** (M21 Phase 0): Pyrnova product copy is formal, precise, and operational — information/scope/state/evidence/action/analytical-meaning first. Slogans, taglines, quirky/clever/inspirational/rhetorical headings, jokes, anthropomorphic or faux-dramatic AI language, startup motivational copy, vague benefit claims, generic AI prose, unnecessary adjectives, and generic phrases ("unlock opportunity", "stay ahead", "turn insights into action", "navigate uncertainty", "make smarter decisions", …) are prohibited across dashboards, reports, briefs, notifications, search results, AI-generated analysis, dossiers, exports, and the operator UI. Acceptable headings are literal/functional (Company Intelligence, Threat Intelligence, Company Exposure, Material Changes, Evidence, Relationship Analysis, Source Operations, Outcome History, Intelligence Gaps, Run Assessment, Export Brief, …). | Copy quality is now part of feature acceptance: a correct feature with prohibited language is not complete. Locks a consistent intelligence-grade voice before customer-facing productization (roadmap area 13). Codified in `01-PROJECT-AUTHORITY.md` (Product language authority). |
| D-042 | 2026-09-09 | **Strategic capability reconciliation + phase control** (M21 Phase 0): the broader customer-facing intelligence capability set discussed since M20 — Company Intelligence Dossier, Company Opportunity + Threat Surface, universal entity search (by name/alias/CIK/UEI/CAGE/DUNS/address/executive/program/PIID/facility/geography/capability/sector), natural-language-assisted structured search, on-demand dossier creation, profile-completeness / intelligence-gap measurement, ingest-time sector/industry/capability/geography classification, read-optimized search/profile projections, counterparty intelligence, multi-tier supply-chain reconstruction, dynamic commercial substitution, strategic dependency mapping, economic blast-radius propagation, customer-specific economic exposure fingerprints, standing intelligence requirements + event-triggered escalation, opportunity/threat decay, market anomaly detection, and calibrated forecasting — is preserved durably in `docs/strategy/STRATEGIC_CAPABILITY_ROADMAP.md` (areas 16–22 + the combined-depth doctrine), not implemented in M21. **Phase-control rule (binding):** capabilities discovered during development do not expand the current phase unless required for correctness, safety, architectural integrity, or existing acceptance criteria; otherwise document → roadmap → defer. | Nothing strategically material is silently dropped (roadmap governance rule) and no milestone silently balloons into a product-design exercise. M21 stays a bounded evidence/relationship-diversity milestone. |
| D-043 | 2026-09-09 | **M21 flagship = raw authoritative contract termination + new economic relationship type.** The flagship adverse event is a real USAspending contract **termination for convenience** (PIID `36C25726N0240`, VA Kerrville VAMC boiler replacement; mod `P00002`, −$3,908,263.25, action_type `F`, 2026-08-31) archived as **raw response bytes** (`examples/real_evidence/usaspending_award_termination_dap.*`), upgrading M20's curated-only SEC extract. Deterministic exposure is the recipient UEI `YR7CLZFGCM95`; the new economic relationship is **`SUBSIDIARY_OF`** grounded from the authoritative USAspending recipient hierarchy (child UEI `YR7CLZFGCM95` → parent UEI `KMSLVW1MZWU9`), never from name similarity. Severity follows the frozen dollar bands ($3.91M → LOW/MODERATE band); confidence is HIGH (OBSERVED catalyst + deterministic native-id exposure). Later outcome is honestly UNRESOLVED (termination is days old; no independent later evidence knowable within budget — D never forces a label). | Closes M20's stated gap (source-native termination + relationship-type diversity beyond `SUBCONTRACTOR_OF`/`COMPANY_TO_PROGRAM`). Frozen `scoring_v1`/`fit.py`/severity bands untouched; additive only. |

| D-044 | 2026-09-09 | **Long-term thesis recorded as architectural direction, not Phase 1 scope.** Pyrnova aims to become a continuously updated, historically auditable model of what external change means economically to a specific organization (may eventually span policy, capital, procurement, regulation, corporate activity, supply chains, physical events, markets, geopolitics, facilities, dependencies, opportunities, threats, outcomes). This is long-term direction preserved for optionality (roadmap areas 15–22); it does not authorize building breadth now. | Keeps the ambition durable without letting it expand Phase 1. Informed by the 2026-09-09 red team review (`docs/research/2026-09-09-RED-TEAM-REVIEW.md`). |
| D-045 | 2026-09-09 | **Phase 1 customer wedge locked: US federal contractors**, sectors defense/industrial/technology/engineering/infrastructure, working ideal customer ≈ **$50M–$300M revenue** with lean BD/capture capacity and insufficient monitoring bandwidth. This refines and **supersedes** the earlier "~$20M–$250M" README figure. A launch wedge only; the architecture must not foreclose other markets (roadmap area 15). | Narrows the launch to a finishable, validatable wedge. Feasible from open/authoritative data (`docs/research/2026-09-09-STRATEGIC-DATA-INTELLIGENCE.md`). Supersedes older README customer copy. |
| D-046 | 2026-09-09 | **Material-change/consequence/customer-relevance loop is the Phase 1 differentiated product.** Dominant customer question: "What materially changed since I last looked?" **Material Changes is the dominant UX**; the dossier (Company/Program Intelligence) is a supporting investigation surface; the **graph is predominantly hidden** from the primary workflow. Pyrnova is not differentiated by breadth, generic search, dashboards, or alerts alone. Detailed surfaces and the required EVENT→ENTITY→RELATIONSHIP→CONSEQUENCE→OPPORTUNITY/THREAT→EVIDENCE→REVIEW→OUTCOME loop are in `docs/strategy/PHASE_1_PRODUCT_AUTHORITY.md`. | Locks the differentiated experience so productization does not drift into a generic search/dashboard product. Capture Radar remains the engine; this organizes it around the loop. |
| D-047 | 2026-09-09 | **Finishability rule (binding; hardens D-042).** A missing capability may block a phase only if correctness, safety, architectural integrity, existing acceptance criteria, or real Phase 1 customer decision value requires it. Research curiosity, elegance, theoretical usefulness, competitor parity, "while we are here", an interesting dataset, or an agent noticing another useful relationship/event/source do not qualify. Default: DOCUMENT → ROADMAP → DEFER. | Finishability is the greatest founder-controlled risk (red team review). Codified in `01-PROJECT-AUTHORITY.md`, `PHASE_1_PRODUCT_AUTHORITY.md`, and `AGENTS.md`. |
| D-048 | 2026-09-09 | **Phase 1 explicit non-goals recorded as durable/deferred/customer-gated.** Deferred unless later real Phase 1 customer evidence changes authority: Dataminr-equivalent global detection, broad social-media ingestion, commodities/maritime/aviation intelligence, global multi-tier supply-chain reconstruction, automated replacement-supplier claims, comprehensive private-company universe, complete beneficial ownership, global facilities completeness, strategic-intent/buyer-intent prediction, advanced portfolio modelling, state/local procurement, complete financial terminal, deep person intelligence, unrestricted autonomous agents, autonomous commercial action, broad premium-feed integration, unnecessary graph-database migration, premature streaming infrastructure. Preserve architectural compatibility; do not implement. | Prevents research-indicated "eventually useful" capabilities from silently entering Phase 1. Full list in `PHASE_1_PRODUCT_AUTHORITY.md`; roadmap items status-labelled DEFERRED/CUSTOMER-GATED. |
| D-049 | 2026-09-09 | **Defensibility / moat authority.** Candidate durable moats (priority order): historical "what could have been known then?" state; customer-specific exposure/outcome history; longitudinal outcome calibration; rights-safe relationship history; rejected-intelligence history; customer-contributed context; accumulated event/consequence performance; evidence and decision lineage. NOT durable moats by themselves: AI, prompts, agents, dashboards, graph visualization, natural-language search, dossiers, alerts, RAG, citations, generic entity resolution, generic knowledge graphs. | Directs long-term investment toward accumulated history/calibration rather than commodity capabilities. Consistent with the existing "stored data is not itself proof of a moat" boundary. |
| D-050 | 2026-09-09 | **Research→decision traceability established.** `docs/research/00-RESEARCH-INDEX.md` records each strategic research artifact with its conclusions and the decisions it caused; strategically significant decisions reference their informing research. The 2026-09-09 red team review and strategic data research are archived under `docs/research/` (findings preserved, not only summarized; original standalone verbatim artifacts were not present at sync time and are noted as such rather than fabricated). | Research must not become a graveyard of long reports; every strategic conclusion is traceable to a decision. Applies to strategically significant decisions only (no bureaucratic overhead for minor ones). |
| D-051 | 2026-09-09 | **M21 formally CLOSED at final HEAD `7446c659758fa37fe1ff1235d95bc079d3bc9221` (482 tests passing); the next phase (M22) is customer-facing Phase 1 productization**, direction set by the 2026-09-09 authority synchronization and `PHASE_1_PRODUCT_AUTHORITY.md`. Direction is authoritative; **implementation is not yet authorized** — `02-EXECUTION.md` must open the milestone before work begins. This records the pivot from backend expansion to customer-facing productization. | Makes the post-M21 pivot durable and unambiguous while preserving the STOP-and-await-brief gate; no product code changed by the synchronization. |

| D-052 | 2026-09-09 | **Competitive doctrine adopted as binding strategic authority** (`docs/strategy/COMPETITIVE_DOCTRINE.md`). Pyrnova competes to displace incumbents, not to reach parity: the Phase 1 commercial test is whether Pyrnova repeatedly tells an existing GovWin/GovTribe/equivalent customer something materially important their workflow missed, early and clearly enough to change what they do. Seek CLEARLY BETTER / UNIQUELY CAPABLE / STRUCTURALLY ADVANTAGED positions; assume features are copied and build imitation-surviving advantages (accumulated history, outcome calibration, rejection history, customer-specific exposure, rights-safe relationships, evidence lineage — extends D-049). Attack the adjacent problem incumbents solve poorly; do **not** attack Bloomberg/AlphaSense/Dataminr/Kpler/Altana/Exiger/PitchBook where their structural advantage is overwhelming (rent that evidence instead, D-008). Prefer design customers already using an incumbent; the evidence standard is **displaced spend**, measured per pilot. Governing question for every major Phase 1 capability: "Why would a rational customer already paying the incumbent switch to Pyrnova?" — a weak answer means the feature is not finished. Legal/ethical boundary binding: no unlawful access, misrepresentation, IP theft, system interference, customer-data misuse, license/contract violation, or deceptive practice. | Sets a relentless competitive-superiority standard without authorizing implementation, scope expansion (still gated by D-042/D-047/D-048), or any unlawful/deceptive practice. Sharpens the falsifiable Phase 1 commercial validation the red team review required. |

| D-053 | 2026-09-09 | **Competitive intelligence & strategic data autonomy doctrine adopted** (`docs/strategy/COMPETITIVE_INTELLIGENCE_AND_DATA_AUTONOMY.md`). Competitive intelligence is a first-class capability studied via **lawful means only**. **Binding now (operating discipline):** the proper-means rule (permitted vs. prohibited acquisition — no unauthorized access, credential misuse, malware, bribery, misrepresentation, inducement to breach confidentiality, trade-secret/leaked-code/stolen-database use, or knowing receipt of improperly obtained information); YELLOW edge-case classification → legal review before operationalization; clean-room OBSERVATION/IMPLEMENTATION separation; personnel-intelligence boundary (public professional facts only; no private-vulnerability dossiers, no soliciting confidential info, no inducing breach); strategic data-autonomy assessment per material source (VALUE/COST/RIGHTS/UNIQUENESS/REPLICABILITY/SUBSTITUTION/CUSTOMER-IMPORTANCE/EXIT-COST); and rights-contamination control (separable provenance; "if we terminate tomorrow, what do we lose?" → less every year). **Roadmap/deferred (NOT current authority; gated by 02-EXECUTION/D-047/D-048, roadmap area 23):** living competitor dossiers + change detection, product acquisition/teardown + reproducible benchmarks, and the vendor-displacement program. | Makes competitive study and data independence durable operating discipline without authorizing implementation, scope expansion, or any unlawful/contaminated collection. Extends D-052 (competitive doctrine), D-049 (moat), and D-008 (own the intelligence layer, rent selectively). |

| D-054 | 2026-09-09 | **Corporate posture & brand doctrine adopted** (`docs/strategy/CORPORATE_POSTURE_AND_BRAND.md`). Pyrnova maintains two deliberate faces: **to customers** — professional, discreet, calm, precise, evidence-led, trustworthy, never theatrical/juvenile/threatening (continuous with the Product Language Authority, D-041); **internally/competitively** — adversarial, secretive by default, relentlessly competitive, hostile to unnecessary dependency. Rules of engagement: play hard, exploit every legitimate advantage, stay inside law and contract; where the boundary is uncertain, escalate for review (YELLOW, D-053) rather than retreat. **Secrecy default = NEED TO KNOW for *external* disclosure** (protect roadmap, datasets, methods, dossiers, pricing, weaknesses); this does **not** weaken internal record-keeping — the repository stays the full durable source of truth and "unknown stays unknown / do not fabricate" still bind. Visual/environmental character may convey authority/restraint/controlled menace (no costume-villain/tactical clichés); customer environments remove overt menace. Reputation is earned through execution, not misconduct. | Codifies brand/posture/disclosure character durably. Authorizes no implementation, no scope expansion, nothing unlawful/deceptive, and nothing intimidating toward customers; it governs presentation and external disclosure only. Extends D-041 (product language) and complements D-052/D-053. |

| D-055 | 2026-09-09 | **M22-A Material Changes vertical slice delivered** (`docs/specs/M22A_MATERIAL_CHANGES.md`). First customer-facing productization: a read-optimized Material Changes projection (`pyrnova/material_changes.py`) over existing `threats`/`propagated_threats`/`opportunities`, deterministic customer relevance (DIRECT_SUBJECT/WATCHED_ENTITY/WATCHED_PROGRAM/CAPABILITY_MATCH/AGENCY_INTEREST — no LLM relevance agent), a `/api/material-changes` read path, and a customer-facing Material Changes frontend served at `/` (Operator Console → `/console`). OBSERVED fact and PYRNOVA ASSESSMENT are kept in separate structured blocks (never flattened); materiality (severity) and confidence stay orthogonal; point-in-time truth enforced (`observed_at <= as_of`, with an event-time floor so a threat is never knowable before its triggering event); evidence referenced by id with conservative independence (single authoritative source ⇒ not corroborated); customer isolation enforced. Demo uses REAL archived evidence (M19 SAIC→Torch, M21 DAP termination) via a tracked `examples/material_changes_demo/` seed generated from `examples/real_evidence/` — no fabrication, no live calls. Additive only: `scoring_v1`/`fit.py`/`replay.py`/severity bands and frozen corpora byte-identical; full suite **492 passed** (was 482; +10 M22-A). Documented follow-ups (evidence-lineage independence, real opportunity/monitoring data, persisted non-demo streams, real tenancy boundary) roadmapped, not swallowed. | Makes Pyrnova begin behaving like a product (roadmap areas 13/16/17/18) under the Phase 1 authority and D-041/D-046, without a parallel intelligence system or scope creep past a single vertical slice. See D-046, D-051; M22-A opened in `02-EXECUTION.md`. |

| D-056 | 2026-09-10 | **M22-B persisted customer intelligence + Material Change lifecycle delivered** (`docs/specs/M22B_PERSISTED_CUSTOMER_LIFECYCLE.md`). Removes the M22-A demo seam: customer configuration is now PERSISTED (`pyrnova/customers.py` — `CustomerProfile`, `WatchlistEntry`, `ReviewAction`; append-only JSONL streams `customers`/`customer_watchlist`/`customer_review_actions`, production mirror in `db/schema.sql`) and drives the read path. **Truth-model decisions:** (1) **Global vs customer-private boundary** — customer config/watchlists/relevance/review state are customer-private and are NEVER written into the global intelligence graph (`threats`/`propagated_threats`/`exposures`/`relationships`); (2) **System assessment ≠ customer review state** — a customer's NEW/REVIEWED/MONITORING/INVESTIGATING/DISMISSED/RESOLVED lifecycle is an append-only per-`(customer, change)` overlay that never mutates Pyrnova's authoritative assessment (not a second verdict system); (3) **Tenancy isolation** — every record is `customer_id`-keyed and cross-customer access (acting on another customer's change, retiring another customer's watch) is rejected; (4) **Temporal truth for configuration** — profile versions carry `effective_from` and watches carry `valid_from`/`valid_to`, so point-in-time replay reconstructs exactly what the customer monitored at the cutoff (no retrospective watchlist leakage); (5) **Outcome lineage** — a resolution may link an existing Pyrnova outcome (`outcome_ref`) preserving intelligence→saw→reviewed→acted→outcome, with UNRESOLVED first-class; (6) **No arbitrary canonicalization** — a free-text watch ref is preserved honestly as unresolved, never silently canonicalized. Demo customers (Torch, DAP) are seeded into the persisted structures by a deterministic, idempotent seed (`examples/material_changes_demo/seed_customers.py`; `pyrnova seed-customers`) that lives in `examples/` — never hard-coded into runtime. Minimal frontend lifecycle actions added behind the unchanged M22-A view; persist across refresh/restart. Additive only: `scoring_v1`/`fit.py`/`replay.py`/severity bands and frozen corpora byte-identical; M22-A read/API behavior compatible. Full suite **510 passed** (was 492; +18 M22-B). | Proves Pyrnova can remember who the customer is, what matters to them, what it showed them, and what they did — the customer-specific exposure/outcome/rejection history that is the Phase 1 moat (D-049/D-052) — without CRM/IAM/tenant-platform scope (D-048) or a parallel intelligence system. See D-055 (M22-A), D-046/D-051; M22-B opened + closed in `02-EXECUTION.md`. |

| D-057 | 2026-09-10 | **M22-C per-tenant persisted Material Change streams + production read path delivered** (`docs/specs/M22C_CUSTOMER_MATERIAL_CHANGE_STREAMS.md`). Closes the M22-A/B storage seam: a relevant global change is now MATERIALIZED into durable per-customer state (`pyrnova/customer_material_changes.py` — `CustomerMaterialChange` append-only VERSION; `fan_out`/`rebuild_customer`/`version_history`; customer-scoped JSONL streams `customer_material_changes`/`fanout_runs`; production mirror `customer_material_change`/`fanout_run` in `db/schema.sql`), and ordinary reads serve that state instead of re-projecting the shared global streams. **Truth-model decisions:** (1) **Global truth stays global** — global intelligence (`threats`/`propagated_threats`/`opportunities`/outcomes) is NEVER duplicated as customer-owned truth nor mutated by a customer; fan-out writes ONLY the customer-scoped streams (the global `threats` stream is byte-identical after a run); (2) **References, not copies** — a customer record stores `source_refs` + a compact `assessment_snapshot` (disposition/materiality/confidence/mechanism/lifecycle_state) + relevance basis + first-seen times + delivered outcome state, and the prose projection is reconstructed at read time from the referenced live global record, never stored; (3) **Stable identity** — keyed by `(customer_id, material_change_id)` where `material_change_id` IS the source intelligence id (the M22-B review-action linkage unchanged), storage id `cmc_<hash(customer, change, content_version)>`; two customers sharing a global change keep independent customer-scoped rows; (4) **Three distinct first-seen times, never collapsed** — `intelligence_observed_at` (global knowability), `first_relevant_at` = max(observed, earliest matching-config effective_from), `delivered_at` (first materialization), immutable across versions; "reviewed" remains the separate M22-B lifecycle joined at read time; (5) **Deterministic idempotent replayable point-in-time fan-out** via the SAME `build_material_changes` read model (persisted set never diverges from the on-the-fly relevant set; a re-run over unchanged truth writes nothing; no future or retrospective-watch leakage); (6) **Append/version update semantics** — an assessment change appends a new version, a later outcome appends an `outcome`-kind version preserving prior assessment snapshots (no duplicate cards, no in-place rewrite); rebuild is idempotent and never erases the separate customer review lifecycle; (7) **Per-customer/per-item failure isolation** with a structured observability report appended to `fanout_runs`. Read path: `OperatorConsole` gained an OPTIONAL `cmc_store` (first-seen overlay + `materialized` count; `None` ⇒ exactly M22-A/B, fully backward compatible) and an OPTIONAL `access_check` authorization seam surfacing `PermissionError` as HTTP 403 — **authentication itself is DEFERRED (D-048); only the seam exists.** Fan-out is the ordinary continuous-operations path: the server runs `fan_out()` at startup and `pyrnova fanout` is the CLI; API adds `POST /api/fanout` and `GET /api/material-changes/{id}/versions?customer=<id>`. Additive only: `scoring_v1`/`fit.py`/`replay.py`/severity bands and frozen corpora byte-identical; M22-A/B read/API behavior compatible. Full suite **532 passed** (was 512; +20 M22-C). | Gives customer isolation a STORAGE boundary (not just a relevance/overlay filter) and makes Pyrnova keep a durable per-tenant record of what it delivered, when it became relevant, and how the assessment evolved — the accumulated exposure/outcome history that is the Phase 1 moat (D-049/D-052) — without duplicating the intelligence graph, a tenant platform, or auth (D-048). See D-055 (M22-A), D-056 (M22-B), D-046/D-051; M22-C opened + closed in `02-EXECUTION.md`. |
| D-058 | 2026-09-10 | **M22-D company/program investigation pages + deterministic entity search delivered** (`docs/specs/M22D_INVESTIGATION_SEARCH.md`). Completes the first investigation path beneath a Material Change (MATERIAL CHANGE → AFFECTED COMPANY/PROGRAM → INVESTIGATION PAGE) and DETERMINISTIC SEARCH → ENTITY/PROGRAM RESOLUTION → PAGE. New `pyrnova/investigation.py`: an `IntelligenceEstate` **computed read projection** (same pattern as `build_material_changes` / Operations-Panel views — NOT a second persisted truth system) built deterministically, point-in-time, from the existing global streams and reusing the shared `material_changes` normalizers so the investigation view never diverges from the feed; `search()`; `company_intelligence()`/`program_intelligence()`. **Decisions:** (1) **No second entity-resolution system (§10)** — entities are the canonical refs already in the graph (`co_*`/`co_uei_*`), programs are source-native keys; identifiers are read from STRUCTURED provenance only (`co_uei_` ref shape, `SUBSIDIARY_OF` hop provenance, and `uei:`/`cik:`/`cage:`/`lei:` tokens on a *direct* single-subject threat); a bare token on a multi-entity record is never harvested; missing identifiers stay missing (never fabricated) and `_merge_ident` never overwrites an asserted value. (2) **Deterministic search, no runtime LLM (§6/§9)** — fixed order exact-identifier → exact-name/alias → scored-partial, resolving to EXACT/PROBABLE/AMBIGUOUS/UNRESOLVED; exact identifiers by dictionary lookup; no import from `pyrnova.ai` (verified by a poison test). (3) **No silent canonicalization (§7)** — two same-named entities are surfaced as two (AMBIGUOUS), never merged; partial scoring has a coverage floor so a single common token does not mint a match. (4) **Reversible resolution (§11)** — search reads/classifies, never writes a merge or mutates identity; a query is not evidence of identity. (5) **Global vs customer boundary (§12/§23)** — pages are global and carry no customer context by default; an authorized `customer` adds a separately-keyed `customer_context` overlay via the existing M22-C `access_check` seam (unauthorized → HTTP 403), never folding private relevance into global truth nor leaking across customers (direct negative tests); **authentication still DEFERRED (D-048).** (6) **Temporal truth (§7/§19)** — estate + sections reconstructed at `as_of`; relationship `valid_from`/`valid_to` respected; no future/outcome leakage. (7) **UNKNOWN is a feature (§17)** — sparse intelligence stated honestly (no UEI/CAGE/CIK, ownership unresolved) rather than hidden or inferred; evidence referenced, never copied (§18). Read path: `OperatorConsole.search`/`company_intelligence`/`program_intelligence` + an `investigation` link block on every Material Change (affected company/program/related — the customer never copies an identifier). API `GET /api/search`·`/api/company?ref=`·`/api/program?key=`; restrained `investigation.{html,js,css}` (one asset trio routed by path) linked from the Material Changes masthead + per-card; CLI `pyrnova search`. Additive only: `scoring_v1`/`fit.py`/`replay.py`/severity bands and frozen corpora byte-identical; M22-A/B/C read/API behavior compatible. Real-evidence demo (no live calls): `co_saic`/UEI `KMSLVW1MZWU9`/UEI `YR7CLZFGCM95`/PIID `47QFSA20F0057` resolve EXACT; `DAP Construction Management` resolves AMBIGUOUS across the real child and its native-id parent. Full suite **557 passed** (was 532; +25 M22-D). | Makes Material Changes lead somewhere useful — the investigation surface that turns a change into a decision — while proving the deterministic retrieval substrate that a future NATURAL LANGUAGE → STRUCTURED QUERY → DETERMINISTIC RETRIEVAL capability sits on, without a parallel entity model, a runtime LLM dependency, new sources, or auth. See D-055 (M22-A), D-056 (M22-B), D-057 (M22-C), D-046/D-051; M22-D opened + closed in `02-EXECUTION.md`. |

| D-059 | 2026-09-10 | **Core engineering & infrastructure doctrine codified as binding architectural authority** (`docs/architecture/ENGINEERING_DOCTRINE.md`, `docs/architecture/INFRASTRUCTURE_DOCTRINE.md`; discoverable from `00-INDEX.md`, `AGENTS.md`, `docs/architecture/README.md`). AI-assisted development does not lower engineering standards. **Software-engineering doctrine (binding now):** decision order (best practice → existing Pyrnova pattern → simplest correct → readable → testable → low coupling → measured performance → only then clever optimization); existing patterns are default authority (deviation must be recorded); one concept, one canonical implementation (duplicate truth is a defect); simplicity over premature abstraction; small coherent functions and explicit state ownership (a read function must not mutate durable state); explicit data flow preserving provenance/temporal/customer/observation-vs-inference/identity; type where mistakes are expensive; intentional error handling (no broad silent `except`); **no silent fallback that changes semantics — UNKNOWN over false certainty**; dependency discipline (mature, boring, justified); no dead code in production paths; no speculative generalization; correct→clear→measured→optimized; comments explain WHY; risk-proportional testing (count is not quality); separate behavioral change from broad refactor; no permanent TODO sediment; one-engineering-organization consistency; debuggability-first; the code-review question ("merely working, or the best-practice Pyrnova way?"); static quality controls without toolchain theatre; and a completion standard beyond happy-path/green-tests. **Infrastructure & capital doctrine (binding now):** capacity follows utilization, reliability follows consequence; permanent (canonical/native IDs, semantics, provenance, hashes, bitemporal truth, customer isolation, Material Change identity, adjudication/outcomes, replay, domain/API contracts) vs replaceable (Postgres/object-store/compute/queue/scheduler/search/analytics/secrets/observability providers); staged Stage 0–4 evolution spending only as reached; PostgreSQL direction with **no rewrite-based scaling**; object evidence behind a stable archive abstraction; disposable stateless compute (no premature k8s/Kafka); migration doctrine (parallel → backfill → validate → shadow → canary → cutover → rollback → retire; never switch-and-hope); expand-contract schema migrations; fourteen migration invariants that must be provably preserved or the migration does not ship; backups unproven until restore-tested; failure testing; consequence-scaled observability; and an 11-question infrastructure change gate. **Mantras:** *start cheap, architect expensive; prefer boring, explicit, testable engineering; one concept, one canonical pattern; no vibecoded foundations; no silent semantic fallbacks; no rewrite-at-scale handwaving; infrastructure may change, intelligence semantics must not; optimize for the engineer debugging this later.* Documentation-only; no code, scoring, or infrastructure changed by adoption. | Codifies durable engineering/infrastructure authority so future AI agents and human developers cannot drift into vibecoded foundations, duplicate architecture, silent semantic fallbacks, or capacity ahead of evidence. Extends the core doctrine in `01-PROJECT-AUTHORITY.md`, the finishability rule (D-047), the copy-quality-as-acceptance rule (D-041), and the Phase 1 infrastructure non-goals (D-048). Architectural-authority level: never overrides current product/strategic authority, the milestone work order, or durable decisions above it. |

| D-060 | 2026-09-10 | **M22-E opportunity Material Changes delivered — the customer-facing product is no longer threat-only** (`docs/specs/M22E_OPPORTUNITY_MATERIAL_CHANGES.md`). WIRING/MATERIALIZATION milestone connecting Pyrnova's already-proven opportunity engine to the customer-facing Material Changes product — **no new opportunity engine, model, stream, or persistence**. **Root cause of threat-only:** the read model (`build_material_changes`) and the M22-C `fan_out` both already consumed an `opportunities` stream, but the demo seed produced only `threats.jsonl`/`propagated_threats.jsonl` — there was never any opportunity data to show. **(1) Authoritative source (§39/§43):** the existing deterministic `detect_recompetes` engine (`pyrnova/engines/recompete.py`) producing the existing `Opportunity` model into the existing global `opportunities` stream. **(2) Real evidence, archive-first (§47):** Torch's own archived USAspending awards (`examples/real_evidence/usaspending_torch.json`) → `recompete_expiry` opportunities on Torch's expiring incumbent contracts (DIRECT_SUBJECT); a principled $100M materiality floor + 540-day window at a pinned scan date (2026-09-01) yields 6 deterministic, byte-reproducible real opportunities. No live calls, no fabrication. DAP has no active recompete and honestly stays threat-only (also proving isolation). **(3) Deterministic identity (§43):** `opp_<sha256(award_id, catalyst_kind, subject_ref)[:20]>` — stable across rebuild/fan-out, source-linked, not prose-based (the pipeline's random `_uid` is not used for materialized records; catalyst/evidence ids also pinned). **(4) Two genuine defects fixed:** `_opportunity_change` conflated the tenant `customer_id` with the affected canonical entity — now the opportunity carries the canonical `subject_ref` (`co_torch`) + `subject_name` distinct from the tenant id, so DIRECT_SUBJECT relevance and investigation links resolve; and opportunities were read from `self.store` while threats/fan-out read from `mc_store` — unified to `mc_store` in the feed and the investigation estate (one canonical pattern, engineering-doctrine §2/§3), a no-op in production (same store) and correct in the demo. **(5) Observed vs assessed (§45):** observed contract facts (incumbent, known value, period-end deadline, agency) stay separate from the forward recompete assessment (confidence, falsification, recommended action); known value is OBSERVED, never an assessed severity (materiality stays UNKNOWN). **(6) Temporal truth (§48/§49):** `observed_at` = pinned in-window scan date (never earlier than knowable); `expected_action_at` = real period-end (future = legitimately active); a cutoff before the scan date hides it; a far-future scan yields no candidate (expired ≠ active) — all tested. **(7) Reuse (§46/§52/§53/§54):** `assess_relevance`, M22-C `fan_out`/customer-scoped storage, and the M22-B lifecycle reused unchanged — no parallel opportunity lifecycle; outcome linkage preserved with UNRESOLVED first-class; `first_relevant_at` never precedes observation. **(8) Frontend (§55):** `material.js`/`material.css` already rendered OPPORTUNITY; added only the necessary distinctions (known contract value, expected-action deadline) beside THREAT — no redesign, no separate dashboard. **(9) Copy fix (§56):** `threat.py` economic-effect names the affected entity explicitly instead of the ambiguous "the subject's revenue" (id derives from `(subject_ref, mechanism, anchor)`, not copy — no id ripple; consequence not in the customer-scoped content hash — no spurious versions). Additive only: `scoring_v1`/`fit.py`/`replay.py`/severity bands and frozen corpora byte-identical; threat/monitoring behavior unchanged; M22-A/B/C/D read/API compatible. Full suite **571 passed** (was 557; +14 M22-E). | Closes the design-customer readiness P0 that the product was threat-only: Pyrnova now materializes real, evidence-backed OPPORTUNITY Material Changes through the same deterministic, customer-scoped, point-in-time, isolated path as threats — proving the OPPORTUNITY/THREAT/MONITORING symmetry the architecture always supported was simply never wired. Connects the engine Pyrnova already proved to the product customers will use, without a second engine, new sources, or auth. See D-055 (M22-A), D-056 (M22-B), D-057 (M22-C), D-058 (M22-D), D-046/D-051, D-059 (doctrine); M22-E opened + closed in `02-EXECUTION.md`. |
| D-061 | 2026-09-10 | **M22-F minimal customer access + seed-free onboarding delivered** (`docs/specs/M22F_MINIMAL_ACCESS_ONBOARDING.md`). Attaches a real authenticated identity to the previously-deferred (D-048/D-057/D-058) `access_check` authorization seam — **the smallest serious access model** that closes the design-customer access P0 — **without enterprise IAM**. **(1) Authentication (`pyrnova/access.py`):** a high-entropy bearer credential `Authorization: Bearer <credential_id>.<secret>` (256-bit CSPRNG secret; public non-secret `cred_<hex>` id embedded so auth is an O(1) single-record lookup); append-only `credentials` stream, PG mirror `credential` in `db/schema.sql`. **Only a salted one-way hash** of the secret is persisted (`sha256(salt‖secret)`; plaintext shown once at creation, not recoverable); constant-time compare (`hmac.compare_digest`); revocation appends a closure (`status='revoked'`) that fails auth immediately, never a destructive delete — no custom cryptography (stdlib only; a slow KDF is pointless for a full-entropy token). **(2) Actor ≠ tenant (§48/§49):** an `AuthContext` (`credential_id`/`role`/`customer_id`/`actor_label`) is the ONLY thing handlers consume — a CUSTOMER credential is scoped to one tenant, an OPERATOR credential is Pyrnova-internal (no single-tenant scope) — so a later OIDC/SSO/SCIM layer replaces only `authenticate()`, not product authorization. **(3) Server-enforced tenant isolation, fail closed (`ops_server.py`):** the customer NEVER comes from a request parameter/dropdown when the credential determines it — a forged `?customer=` mismatch is a hard 403 (never a fallback or partial cross-tenant read); per-request thread-local context + route levels (PUBLIC app shell / CUSTOMER tenant-scoped / GLOBAL any-authenticated / OPERATOR internal); defense-in-depth binds the console's `access_check` to the request actor. **(4) Customer enumeration eliminated (§13):** an ordinary customer's `/api/customers` returns only itself (+ `/api/me`); the all-tenant listing is operator-only. **(5) Remote binding + operator separation (§11/§16):** `AccessPolicy` forces auth ON for any non-local bind / `PYRNOVA_REQUIRE_AUTH` / any provisioned credential (only a local checkout with no credentials stays permissively dev — never silently for remote); the operator surface (snapshot/fan-out/briefs/adjudication/all-customer listing+creation/`/console` assets) is exposed only on a local bind (404 remotely) — the localhost-only operator surface §11 permits, with production edge/TLS assumed in front (no Cloudflare/TLS/proxy built). **(6) Frontend (§10/§36/§37):** an Access gate (`ops_web/access.js`/`access.css`), credential in sessionStorage (never a URL), `Pyrnova.authFetch` bearer wrapper returning to the gate on 401, Sign Out; the **customer dropdown is removed** — the authenticated organization is shown and cannot be switched; the investigation overlay uses the authenticated customer, never a URL param; no auth in frontend only — every boundary is server-enforced. **(7) Seed-free onboarding (`pyrnova/onboarding.py` + CLI `pyrnova customer|watch|credential`):** an operator onboards a customer without editing seed/Python/JSONL, REUSING the M22-B `CustomerProfile`/`WatchlistEntry` (ENTITY/PROGRAM/CONTRACT/AGENCY; `valid_from`/`valid_to` temporal truth; cross-customer-safe retirement) and M22-D deterministic `search` (EXACT selected / PROBABLE needs explicit `--accept-probable` / AMBIGUOUS never silently chosen / UNRESOLVED never fabricated — §20/§54), then the unchanged M22-C fan-out; a new watch never backdates relevance (§52, tested). Additive only: `scoring_v1`/`fit.py`/`replay.py`/opportunity engine/materiality-value-severity bands and frozen corpora byte-identical; M22-A→E read/API behavior compatible (permissive local-dev posture preserves it; `make_handler` gained optional `policy`/`auth_store`). Full suite **615 passed** (was 571; +44 M22-F). **Deferred (DOCUMENT→ROADMAP→DEFER, D-048):** SSO/SAML/OIDC/SCIM, MFA, enterprise RBAC, invitations/self-service signup, password reset/email verification, billing/subscriptions, audit export, SOC 2 tooling, customer-managed keys, full offboarding automation, per-request `last_used_at`, production TLS/edge, PostgreSQL-migration/Redis. Not claimed enterprise-grade/zero-trust/SOC-2/production-hardened. | Closes the design-customer ACCESS + ONBOARDING P0: a real design customer can receive a credential, access Pyrnova remotely, see/modify ONLY their authorized tenant, and be onboarded without code — with tenant isolation demonstrably tested and enterprise IAM explicitly deferred. Opportunity side closed by D-060; data-volume/live-ops remains the separate next task. See D-055 (M22-A), D-056 (M22-B), D-057 (M22-C), D-058 (M22-D), D-060 (M22-E), D-059 (doctrine), D-048 (non-goals); M22-F opened + closed in `02-EXECUTION.md`. |
| D-062 | 2026-09-10 | **Canonical product + commercial authority established** (`docs/strategy/PRODUCT_COMMERCIAL_AUTHORITY.md`, owner-approved). Unifies the current product, commercial/pricing, trust, validation, UX, AI, and expansion doctrine into one overarching tier-1 authority sitting above `PHASE_1_PRODUCT_AUTHORITY.md` and `COMPETITIVE_DOCTRINE.md` (consistent companions that defer to it on any direct conflict); wired into `00-INDEX.md` + `AGENTS.md` reading order/hierarchy. **New durable commercial model:** premium mid-market intelligence product; paid design pilot **$12,500 target / $10,000 founder floor / 10 weeks / 50% at signature + 50% at midpoint**; **50% pilot→annual conversion credit** if signed within 30 days; initial annual **ACV $48K target / $36K floor / $72K stretch**; pricing unit is licensed **organizational intelligence scope**, not alert/search/token/seat/record; **one Phase 1 product** (premium data/API/services are explicit exceptions). Reaffirms: operational ICP ~$50M-$300M US federal contractors ($25M-$500M broader addressable); Material Changes is the primary decision surface at ~5-12 validated items/customer/week; AUGMENT->ATTENTION->WORKFLOW->BUDGET->SYSTEM-OF-INTELLIGENCE displacement; differentiation is customer-specific consequence/temporal truth/outcome learning, NOT AI; missed-intelligence is first-class; staged trust gates (privileged/admin MFA pre-pilot, customer MFA pre-production, SOC 2/SSO/SCIM later); no high-sensitivity/CUI data under the Phase 1 boundary; five-customer falsification gate before broadening; customer-private state stays out of the global corpus; bad revenue refused. **Supersedes as non-authoritative** (authority sections 23-24) the earlier research hypotheses of a $5K standard pilot, 100% conversion credit, an unresolved $20K-$50K ACV, procurement/opportunity-only identity, dashboard-first Phase 1, AI-as-moat, and pre-pilot full customer MFA/SSO/SCIM - preserved as history in `docs/research/`, not deleted. Documentation-only; no product code, scoring, corpora, tests, or runtime state changed; M21/M22-A-F remain CLOSED (615 passed); data-volume/live-ops readiness remains the open P0. The 22 numbered decisions in the authority's section 25 are recorded there and not re-numbered into this ledger. | Establishes ONE current product/commercial truth with an unambiguous hierarchy so future agents read commercial/pricing/trust/expansion doctrine from a single owner-approved source before consulting research or roadmap, without reopening closed milestones or authorizing implementation (`02-EXECUTION.md` alone does). Extends the Phase 1 product authority (D-044...D-051), competitive doctrine (D-052), and corporate posture (D-054); architectural-authority and current-state records are unchanged and continue to govern technical invariants and live status. |
| D-063 | 2026-09-12 | **Authority-history divergence reconciled by scope, preserving both accepted authorities.** Owner decisions and the Phase One Constitution are highest. `PRODUCT_COMMERCIAL_AUTHORITY.md` governs overarching cross-phase product and commercial strategy. `PHASE_1_PRODUCT_AUTHORITY.md` is the sole product and implementation authority within Phase 1 scope; it may specialize the cross-phase authority for Phase 1 but may not erase, redefine, or supersede it outside Phase 1. Execution, state, milestone, and evidence documents implement or record higher authority; `02-EXECUTION.md` alone authorizes active implementation. | Resolves the divergent local and origin histories without deleting either authority or reopening product strategy. |

| D-064 | 2026-09-12 | **Phase 1 soak machinery composes existing source/archive/pipeline/fan-out patterns and fails closed before the acceptance clock.** Source state owns only recoverable latest health/retry/pending-processing state; the soak evidence directory owns the immutable manifest and append-only cycle/intervention history. Source checkpoints advance only after archived acquisition, deterministic processing, and customer fan-out complete. A stable persisted `customer_id` is explicit on unattended pipeline runs; Important Miss records use the existing append-only StateStore and seven locked miss classes. No scoring, source-rights, replay, materiality, or customer configuration is inferred or changed. | Closes the smallest engineering gaps for automatic operation, retry recovery, explicit health/freshness, miss capture, and supervision without a second intelligence model or orchestration platform. The soak remains blocked until the owner-approved IronMountain Lens exists; substituting or inventing customer-private scope cannot start acceptance. |

| D-065 | 2026-09-12 | **IronMountain is solely a source-backed operational-soak test Lens, not a commercial customer.** Owner accepts the Live Ops implementation conditionally and authorizes `IRONMOUNTAIN SOLUTIONS, LLC` as `SOAK TEST LENS — NON-CUSTOMER / NON-COMMERCIAL`; it is not Customer #1, a design customer, a fixed CUSTOMER-001 cohort member, a substitute for MTSI, or an outreach target. Persisted ID `soak-ironmountain-solutions` reuses canonical onboarding/provenance/watch state. Six source-backed company/contract/agency objects and literal award-description keywords are used; unsupported NAICS/PSC and commercial preferences remain unset. Foreground validation cannot start the official clock; only a verified supervised start does. | Incorporates the highest-authority owner clarification without reconstructing the commercial cohort or tuning intelligence. Corrects test-Lens labeling/copied-plan paths, gates a clean tracked commit, and keeps credential-bearing transport error text out of the ledger. The literal seven-calendar-day/five-business-day operational acceptance remains outstanding; configuration and locked gates are in `docs/operations/PHASE1_SOAK_UNBLOCK.md`. |
| D-066 | 2026-09-15 | **PRELAUNCH-CONVERGENCE-001 is a mandatory pre-release work order** (canonical spec `docs/specs/PRELAUNCH_CONVERGENCE_001.md`). Pyrnova may not be declared pre-launch complete / Customer #1 production ready, or activated for production Customer #1, while any mandatory CLOSE item is materially incomplete; NARROW items must meet a bounded launch floor; REMAIN items are deferred and do not block release. The governing acceptance measure is customer usefulness ("does Pyrnova show a capture executive something consequential, credible and early enough that it changes what they do?"), refining the accepted intelligence lineage via the SIGNAL→…→OUTCOME→LEARNING decision chain. Final Live Ops acceptance is revised: it must run on an immutable pinned release/worktree isolated from development (the mutable canonical checkout must never again be the acceptance runtime root), for 72 consecutive unattended hours spanning ≥3 business days, with deterministic restart/checkpoint/archive-recovery/source-degradation/idempotency/isolation/scheduling/failure-visibility exercises. Does not reopen Phase One strategy, the commercial offer, the Customer #1 order, or website/payment/source-rights/Trust Pack/Company Ready/temporal-provenance doctrine. Gate 0 test adjudication: `docs/operations/GATE0_TEST_ADJUDICATION.md`. | Establishes the pre-release convergence gate and the customer-usefulness acceptance standard, and pins the acceptance-runtime isolation rule so a repeat of the mutable-root soak failure cannot recur. See D-064/D-065; supersedes the prior "final Live Ops acceptance" cadence only as to runtime isolation and duration, not the frozen historical soak evidence. |

The original strategic adjudication and 30-day authority are preserved under `docs/research/` and
`docs/archive/` for provenance; they no longer control current execution. The 2026-09-09 red team review
and strategic data research (D-044…D-051) are indexed in `docs/research/00-RESEARCH-INDEX.md`. The
competitive doctrine (D-052) is `docs/strategy/COMPETITIVE_DOCTRINE.md`; the competitive-intelligence &
data-autonomy doctrine (D-053) is `docs/strategy/COMPETITIVE_INTELLIGENCE_AND_DATA_AUTONOMY.md`; the
corporate posture & brand doctrine (D-054) is `docs/strategy/CORPORATE_POSTURE_AND_BRAND.md`.

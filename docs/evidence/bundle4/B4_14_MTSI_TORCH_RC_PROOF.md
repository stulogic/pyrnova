# B4.14 — Torch / MTSI RC proof (exact candidate)

Integrated, exact-candidate proof for both target customers. Principle enforced throughout:
**EMPTY IS ACCEPTABLE · UNEXPLAINED EMPTY IS NOT · FAKE IS NOT.** No fabricated data; no substitution
of soak-IronMountain data for customer proof.

Two evidence sources, both on the exact candidate SHA:
- **Automated tests** (`tests/`), run under B4.15 full regression.
- **Live capture** `docs/evidence/bundle4/rehearsal_capture.json`, regenerated by
  `rehearsal_capture.py` (fails loudly on any truthfulness violation).

## MTSI

| Requirement | Status | Evidence |
|---|---|---|
| Correct customer identity | PASS | `create_customer("mtsi", …, entity_refs=["co_mtsi"])`; lens id `mtsi`. Capture + `test_b3_mtsi_torch_proof.py::test_mtsi_lens_surfaces_its_real_recompetes_tenant_isolated`. |
| Customer profile | PASS | capabilities/agencies from accepted profile; canonical entity only. |
| Ingestion / fanout execution | PASS | `test_b4_mtsi_fanout.py` — real `detect_recompetes` over `usaspending_mtsi.json` with the same accepted params as Torch. |
| Actual persisted opportunity state | PASS | **2** opportunities in `state/opportunities.jsonl` (`customer_id=mtsi`). |
| Per-opportunity decision path | PASS | first opp **PURSUE / MEDIUM**, evidence-backed (B4.1 recompute). |
| Truthful empty state when none qualifies | PASS | AS-OF `2026-08-01` → **0** (honest empty before first-seen); `test_b4_mtsi_fanout.py` proves fan-out ran. |
| No unexplained empty state | PASS | populated when in-window; empty only explained by AS-OF / fan-out. |
| Correct tenant isolation | PASS | MTSI/Torch opportunity ids disjoint; MTSI evidence only from `usaspending_mtsi.json`. |
| Source-rights-safe evidence | PASS | usaspending only; archive_uri is the MTSI archive; no cross-tenant copy. |
| Customer Lens | PASS | capture `mtsi.lens_identity`. |
| Opportunities | PASS | capture `mtsi.opportunities` (2). |
| Decision View | PASS | `opportunity_decision` pursuit verdict+confidence+action. |
| Evidence Inspector | PASS | `opportunity_evidence` opens first item (`evidence_inspector_ok`). |
| AS-OF | PASS | count 0 before first-seen, 2 at/after. |
| Disposition → Decision Memory | PASS | `record_opportunity_disposition` persisted (RELEVANT/INVESTIGATE). |
| Search | PASS | `test_b3_customer_product.py` route tests exercise `/api/…?customer=…`; `console.search` deterministic. |
| Brief generation / download | PASS | `build_customer_brief` deterministic (`content_sha256`), rights-aware, no obsolete Sprint copy; download route in `test_b3_customer_product.py`. |
| Delivery-state path | PASS | authorized recipient → `DELIVERED` via in-memory transport (no external send); unauthorized → hard refusal (`test_b3_customer_product.py`). |

## Torch

Accepted populated capability remains intact against the current candidate: **6** persisted
opportunities from `usaspending_torch.json`, first opp **PURSUE / MEDIUM**, evidence-backed, tenant
isolated from MTSI. Evidence: capture `torch_populated_proof`;
`test_b3_mtsi_torch_proof.py::test_torch_lens_is_usable_and_coherent`; full product surface in
`test_b3_customer_product.py`.

## Non-fabrication guarantees

- No seeded fabricated data: all opportunities derive from committed real archived awards via the real
  engine.
- No soak-IronMountain substitution: the soak test lens is a separate non-commercial tenant and never
  appears in customer proof.
- The capture script asserts MTSI incumbent identity and MTSI-only evidence provenance, and fails the
  run otherwise.

All of the above is **IMPLEMENTED / TESTED PRODUCT CAPABILITY**, distinct from PRODUCTION / LIVE-OPS
ACCEPTED (which requires the final soak — not started, not claimed).

# Known limitations and technical debt

Status: descriptive register  
Last reviewed: 2026-09-12

Only repository-evidenced items belong here. Priority means engineering attention, not automatic
implementation authority. “Launch-blocking” is scoped to the deployment being attempted; no customer or
production GO is inferred.

| ID | Class | Issue and evidence | Consequence | Current mitigation | Priority | Launch-blocking | Proposed future action |
|---|---|---|---|---|---|---|---|
| TD-001 | VERIFICATION / DATA | The DOCS-001 audit reported duplicate `reason` columns in `join_review`, but REPO-RECONCILE-001 verified one `reason` column in every historical schema revision and no duplicate columns in any current `CREATE TABLE` block. The schema is still not applied by the test suite. | The reported duplicate is not an executable defect; clean PostgreSQL apply/upgrade readiness remains unproven. | Local JSONL is the active development store; no runtime PostgreSQL adapter or known persisted PostgreSQL environment depends on this schema. | P1 | Yes for PostgreSQL launch, not for current JSONL operation | Add clean apply/upgrade validation when PostgreSQL activation is authorized; no schema correction is required for the reported duplicate. |
| TD-002 | ARCHITECTURE | `PYRNOVA_DATABASE_URL` and optional `psycopg` exist, but no runtime PostgreSQL repository adapter uses them. | Schema and local behavior can drift; production structured persistence is not wired. | Append-only JSONL is explicit and well tested locally. | P0 | Yes for a PostgreSQL-backed deployment | Implement stable repository interfaces and a shadow/compare migration per infrastructure doctrine. |
| TD-003 | SECURITY | Tenant isolation is application-layer (`ops_server.py` + `OperatorConsole.access_check`); the schema has no RLS policy. | A future direct database path could bypass tenant checks if introduced carelessly. | Server route checks, credential-derived tenant scope, negative cross-tenant tests. | P0 | Yes for any direct/multi-service DB access | Add DB roles/RLS or a comparably strong data-access boundary when PostgreSQL is activated; test both layers. |
| TD-004 | SECURITY | Pilot access is a single-factor bearer token stored in browser `sessionStorage`; no MFA, SSO, SCIM, password lifecycle, or enterprise RBAC. | Token theft through XSS/session compromise grants tenant access until revocation. | High-entropy secrets, salted hashes, constant-time compare, one-time display, append-only revocation, no URL tokens. | P0 | Conditional; acceptable only inside approved controlled-pilot posture | Put TLS/reverse proxy in front, add CSP and security review, then adopt approved IAM/MFA before production trust gate. |
| TD-005 | SECURITY / OPERATIONS | Built-in `ThreadingHTTPServer` provides no TLS, hardened edge, process supervisor, request rate limiting, or production deployment contract. | Unsafe to expose directly to the public internet; weak operational resilience. | Non-local binding forces auth and hides operator UI; docs state an edge/TLS layer is assumed. | P0 | Yes for public production | Define and verify a production edge, TLS, service manager/container, rate limits, health checks, and rollback. |
| TD-006 | OPERATIONS | No implemented backup schedule, restore automation, restore test, PITR, or disaster-recovery evidence for JSONL/S3/PostgreSQL state. | Recovery point/time and evidence integrity are unknown after loss. | Content hashes and append-only local files reduce some corruption ambiguity. | P0 | Yes once customer data has material consequence | Define backup sets, retention, restore order, integrity checks, and run a dated restore drill. |
| TD-007 | OPERATIONS | A repository-owned soak runner, immutable evidence ledger, explicit health/freshness, and launchd template now exist, but the IronMountain Lens is not approved/configured and no seven-day/five-business-day soak or alert-delivery proof exists. | Engineering machinery does not prove unattended Customer #1 operation. | Offline-default scheduler, cadence, budgets, breaker, restart tests, M13 bounded live evidence, Phase 1 preflight/runner. | P0 | Yes for unattended production | Configure the owner-approved Lens, pass preflight/foreground cycle, render supervision, then complete the literal soak; add alert delivery before relying on remote unattended production. |
| TD-008 | DATA / OPERATIONS | Registry reliability values and connectivity notes are dated; some adapters are fixture-only and SBIR is blocked/unverified. | “Active” does not mean currently reachable or production-proven. | Registry separates `active`, `status`, and `reliability`; offline fixtures/archive replay. | P1 | Source-specific | Revalidate with one budgeted acceptance call per approved source and record dated results. |
| TD-009 | DATA | First-class evidence-lineage independence is absent; Material Changes use distinct authoritative-source count conservatively. | Multiple records may not represent independent corroboration. | UI exposes `single_source`; evidence is referenced, not inflated. | P1 | No for controlled use if disclosed | Model origin/lineage clusters and test duplicate/corroborating event semantics. |
| TD-010 | DATA / CORRECTNESS | Some temporal fields use naive UTC ISO strings while replay corpora require timezone-aware values. | Cross-system comparisons and DST/offset handling can become ambiguous. | Tests pin current accepted behavior; `_visible_at` normalizes some naive values against aware cutoffs. | P1 | Before multi-system production | Adopt one timezone-aware UTC serialization contract and migrate compatibly. |
| TD-011 | ARCHITECTURE / PERFORMANCE | `StateStore.read` loads each JSONL stream fully; append/read operations have no multi-process locking or transaction across streams. | Memory and concurrency behavior degrade with volume; partial multi-stream writes remain possible. | Small controlled local datasets; content-hash idempotency and append-only histories. | P0 for volume readiness | Yes at sustained/multi-worker scale | Measure, define volume thresholds, then move through a verified repository adapter/migration path. |
| TD-012 | OPERATIONS / OBSERVABILITY | Telemetry is local structured state; no centralized logs, alerts, traces, SLOs, or on-call escalation exist. Some containment paths deliberately swallow secondary observability errors. | Failures may remain unnoticed; operational acceptance is unproven. | Source health, breaker, budget, fan-out, selectivity, and cost reports are inspectable locally. | P0 for unattended service | Yes for production | Establish minimal alerts and failure dashboards driven by current structured reports; test alert delivery. |
| TD-013 | DEVELOPER EXPERIENCE | No CI workflow, formatter/linter/type-check configuration, coverage threshold, dependency lock, SBOM, or automated dependency/security scan is tracked. | Reproducibility and supply-chain review rely on manual setup and broad version ranges. | Small dependency surface; full pytest suite and explicit doctrines. | P1 | No for local development; diligence concern | Select a minimal toolchain, pin/lock dependencies, and record CI artifacts. |
| TD-014 | TESTING | Real-archive tests skip when local gitignored evidence is absent; loopback test may skip when sockets are unavailable. No browser E2E suite exists. | A green unit suite does not prove real-source, socket, browser, or deployed behavior. | Skip reasons are explicit; request-handler tests avoid sockets; milestone replay evidence is retained. | P1 | Depends on release | Add controlled acceptance environments and browser/runtime checks; keep them separate from offline CI. |
| TD-015 | DOCUMENTATION | `02-EXECUTION.md` and `05-BACKLOG.md` contain status text inconsistent with later sections/current state; milestone documents repeat old “next” language. | A new engineer may reopen closed work or follow stale priorities. | Authority hierarchy says execution/current state outrank backlog/history; DOCS-001 audit flags exact conflicts. | P0 | Yes for autonomous work | Owner-authorized reconciliation after the current branch divergence is resolved. |
| TD-016 | DATA / LEGAL | Registry records rights/status, but no legal opinion, source terms snapshot, DPA, retention schedule approval, or data-processing inventory is present. | Source access/retention assertions may not satisfy enterprise or acquisition diligence. | Proper-means and YELLOW review doctrine; source-specific rights notes; secrets kept out of Git. | P0 diligence | Potentially | Maintain legal records in the corporate data room and link approved decisions by identifier, not privileged content. |
| TD-017 | ARCHITECTURE | `Claim` exists only in the PostgreSQL schema; the Python domain has a bounded `Reasoner` protocol and stores optional AI metadata rather than a first-class claim object/repository. | AI output lineage and validation are less formal than the schema direction suggests. | Default `NullReasoner`; deterministic path works without AI; AI cannot overwrite deterministic fields by contract. | P1 | No while AI remains unused/bounded | Introduce a first-class claim lifecycle only when an authorized use requires persistence. |
| TD-018 | DOCUMENTATION / API | HTTP routes are implementation-defined in `ops_server.py`; no versioned external API contract exists. | External consumers could couple to unstable endpoints. | Current interface is controlled/internal and exercised by route tests. | P2 | No for controlled pilot | Add a versioned API contract when external integration is authorized. |
| TD-019 | DEFERRED BY DESIGN | Phase 1.5 industrial replay is isolated research/evaluation and not wired to production acquisition, identity, relevance, scoring, outcome vocabulary, or runner. | Industrial scenarios cannot be claimed as current product capability. | Integrated evidence paths remain explicitly marked “production implementation locked” and do not import production pipeline/scoring modules. | P2 until authorized | No for Phase 1 | Preserve the evidence/production boundary; promote only through a new execution authority and policy-specific implementation. |
| TD-020 | DOCUMENTATION | No clean-machine senior-engineer onboarding observation has been recorded. | Written instructions may omit environmental assumptions. | Quickstart, handover, repository map, and tests are present. | P1 | No | Ask an unfamiliar engineer to execute the handover test and record dated gaps. |

## Interpretation

The highest-risk concentration is not the deterministic intelligence logic; it is the step from a
well-tested local application to an operated, recoverable, secured, PostgreSQL-backed service. Green
offline tests are strong implementation evidence, but they do not close those production gates.
